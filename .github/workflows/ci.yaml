name: Shipping CI

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  ci-cd-common:
    uses: chanu-devops/reusable-CICD-yaml/.github/workflows/cicd.yaml@main
    with:
      app-type: java
      micro-services: shipping